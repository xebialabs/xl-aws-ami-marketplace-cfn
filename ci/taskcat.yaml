global:
  owner: xl-aws-ami@xebialabs.com
  qsname: ami-xebialabs-devops-platform

  # N.B.: If you run the tests in a different region than us-east-1,
  # add an override for CertificateArn in ~/.aws/taskcat_global_override.json
  regions:
    #- eu-west-2
    - us-east-1

  reporting: true
tests:
  new-vpc-e2e:
    parameter_input: new-vpc-e2e.json
    template_file: install-xl-platform.yaml
